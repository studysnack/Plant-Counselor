# ══════════════════════════════════════════════════════
# Plant Counselor — Backend 의존성 목록
#
# 사용법:
#   pip install -r requirements.txt
#
# 또는 개발 모드 (editable install, 소스 수정 즉시 반영):
#   pip install -e .
#
# ※ pyproject.toml 이 단일 출처입니다.
#   이 파일은 pyproject.toml 의 dependencies 를 그대로 옮긴 것입니다.
# ══════════════════════════════════════════════════════

# ── Web framework ─────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0      # ASGI 서버

# ── Database (Supabase, PostgREST HTTP) ───────────────
supabase>=2.30.0              # supabase-py 클라이언트 (DB 접근 전부 이걸로)
                              # ※ SQLAlchemy/psycopg2 직접 연결은 사용하지 않음

# ── Auth (Supabase JWT 검증) ───────────────────────────
python-jose[cryptography]>=3.3.0   # ES256(JWKS)/HS256 JWT 검증

# ── Config & env ──────────────────────────────────────
python-dotenv>=1.0.0          # 로컬 .env 로딩 (pydantic-settings)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── LLM (Google Gemini) ──────────────────────────────
# 2.6+ required: older versions mishandle the new "AQ."-prefixed AI Studio API
# key format (send it as an OAuth token → 401 ACCESS_TOKEN_TYPE_UNSUPPORTED).
google-genai>=2.6.0

# ── Scheduling ────────────────────────────────────────
apscheduler>=3.10.0            # 주기적 봉우리 자동 전이

# ── Utilities ─────────────────────────────────────────
python-ulid>=2.0.0             # ULID 기본키 생성
cryptography>=42.0.0           # Fernet 암호화 (사용자 API 키 보호)
