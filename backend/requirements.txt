# ══════════════════════════════════════════════════════
# Plant Counselor — Backend 의존성 목록
#
# 사용법:
#   pip install -r requirements.txt
#
# 또는 개발 모드 (editable install, 소스 수정 즉시 반영):
#   pip install -e .
#
# ※ pyproject.toml 이 단일 출처입니다.
#   이 파일은 pyproject.toml 의 dependencies 를 그대로 옮긴 것입니다.
# ══════════════════════════════════════════════════════

# ── Web framework ─────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0      # ASGI 서버

# ── Database (Supabase PostgreSQL) ────────────────────
sqlalchemy>=2.0.0              # ORM
psycopg2-binary>=2.9.0         # PostgreSQL 드라이버

# ── Auth (Supabase JWT 검증) ───────────────────────────
python-jose[cryptography]>=3.3.0   # HS256 JWT 검증

# ── Config & env ──────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── LLM (Google Gemini 2.5 Flash) ────────────────────
google-genai>=1.0.0

# ── Scheduling ────────────────────────────────────────
apscheduler>=3.10.0            # 10분 주기 봉우리 자동 전이

# ── Utilities ─────────────────────────────────────────
python-ulid>=2.0.0             # ULID 기본키 생성
cryptography>=42.0.0           # Fernet 암호화 (사용자 API 키 보호)
