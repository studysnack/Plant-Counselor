# ══════════════════════════════════════════════════════
# Plant Counselor — Backend 의존성 목록
#
# 사용법:
#   pip install -r requirements.txt
#
# 또는 개발 모드 (editable install, 소스 수정 즉시 반영):
#   pip install -e .
#
# ※ pyproject.toml 이 단일 출처입니다.
#   이 파일은 pyproject.toml 의 dependencies 를 그대로 옮긴 것입니다.
#   버전을 변경할 때는 pyproject.toml 을 먼저 수정하고
#   이 파일도 함께 동기화하세요.
# ══════════════════════════════════════════════════════

# ── Web framework ─────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0      # ASGI 서버 (WebSocket·HTTP/2 포함)

# ── Database ──────────────────────────────────────────
sqlalchemy>=2.0.0              # ORM (비동기 세션 사용)
alembic>=1.13.0                # DB 마이그레이션 도구

# ── Auth ──────────────────────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT 생성·검증
passlib[argon2]>=1.7.4             # Argon2 비밀번호 해싱

# ── Config & env ──────────────────────────────────────
python-dotenv>=1.0.0           # .env 파일 로드
pydantic>=2.0.0                # 데이터 검증 (v2)
pydantic-settings>=2.0.0       # Settings 클래스 (.env 자동 바인딩)

# ── LLM (Google Gemini) ───────────────────────────────
google-genai>=1.0.0            # Gemini 2.5 Flash API SDK

# ── Scheduling ────────────────────────────────────────
apscheduler>=3.10.0            # 10분 주기 봉우리 자동 전이

# ── Utilities ─────────────────────────────────────────
python-ulid>=2.0.0             # ULID 기본키 생성 (정렬 가능 UUID)
cryptography>=42.0.0           # Fernet 대칭 암호화 (사용자 API 키 보호)
